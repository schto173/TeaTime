# Copy this file to ".env" (same folder as docker-compose.yml) and change the
# password. Never commit the real .env file.
#
# This is the Mumble "SuperUser" password. The bot uses it to log in and to
# create channels and move players. Use a long random one.
MUMBLE_SUPERUSER_PASSWORD=change-me-to-a-long-random-password
